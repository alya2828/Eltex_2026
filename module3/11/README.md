# Разделяемая память POSIX

## Описание задачи

Реализована программа на языке C, демонстрирующая использование механизма межпроцессного взаимодействия — разделяемой памяти POSIX.

Программа выполняет следующие действия:

- Родительский процесс генерирует набор случайных чисел.
- Записывает данные в разделяемую память.
- Дочерний процесс считывает данные и находит минимальное и максимальное значение.
- Результат записывается обратно в разделяемую память.
- Родительский процесс выводит результат на экран.
- Работа продолжается до получения сигнала SIGINT (Ctrl+C).
- После завершения выводится количество обработанных наборов.

---

## Используемые технологии

- POSIX Shared Memory  
(`shm_open`, `ftruncate`, `mmap`, `munmap`, `shm_unlink`)

- `fork()` для создания дочернего процесса

- Сигналы (`SIGINT`)

- Busy-wait синхронизация (`ready / done`)

---

## Сборка проекта

make

---

## Завершение программы

Для остановки нажмите:

Ctrl + C

После завершения программа:

- корректно завершит дочерний процесс  
- освободит разделяемую память  
- удалит объект shared memory  
- выведет количество обработанных наборов
